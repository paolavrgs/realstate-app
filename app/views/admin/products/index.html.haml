= link_to 'New Product', new_admin_product_path, "data-method": "get"

.card
  .card-header
    %h4.card-title Detalle del Servicio
  .card-body
    .table-responsive
      %table.table
        %thead.text-primary
          %th Imagen
          %th Nombre de la Propiedad
          %th Dirección
          %th Mts cuadrados
          %th.text-right Acciones
        %tbody
          - @products.each do |product|
            %tr
              %td= product.image
              %td= product.title
              %td= product.address
              %td= product.sqft
              %td= link_to 'Show', admin_product_path(product), "data-method": "get"
              %td= link_to 'Edit', edit_admin_product_path(product), "data-method": "get"
              %td= link_to 'Destroy', admin_product_path(product), method: :delete, data: { confirm: 'Are you sure?' }, "data-method": "get"